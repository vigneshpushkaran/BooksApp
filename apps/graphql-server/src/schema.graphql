  type Query {
    hello: String
  }
  type Mutation{
    createPost(author: String!, comment: String!): Post!
  }

type Subscription {
  postCreated: Post
}

type Post{
  author: String!
  comment: String!
}